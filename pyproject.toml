[project]
name = "rtiow-python"
version = "0.1.0"
description = "A small path tracer written in Python, inspired by Ray Tracing in One Weekend."
requires-python = ">=3.12"
dependencies = []

[project.scripts]
raytracer = "raytracer.__main__:main"